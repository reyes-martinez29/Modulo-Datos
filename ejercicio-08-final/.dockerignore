# IMPORTANTE: este .dockerignore debe vivir en la RAIZ del repo, no dentro de
# ejercicio-08-final/. docker-compose.yml usa context: .. (la raiz del repo),
# y Docker solo respeta .dockerignore en la raiz del build context.
# Ver README.md, "Paso 0", para el comando que lo copia a la ubicacion correcta.

data/
*.parquet
*.db
*.csv

__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.mypy_cache/

results/
quarantine/
*.log

.git/
.gitignore
.vscode/
.idea/

.env

**/tests/